{
  "name": "bsbw-crm",
  "version": "1.0.0",
  "private": true,
  "description": "Multi-organization buyer outreach CRM — React frontend, Express + Postgres API",
  "scripts": {
    "install:all": "npm --prefix server install && npm --prefix web install",
    "migrate": "npm --prefix server run migrate",
    "seed": "npm --prefix server run seed",
    "tidy:names": "npm --prefix server run tidy:names --",
    "dev:api": "npm --prefix server run dev",
    "dev:web": "npm --prefix web run dev",
    "build": "npm --prefix web run build",
    "lint": "npm --prefix server run lint && npm --prefix web run lint",
    "smoke": "npm --prefix server run smoke",
    "test:mail": "npm --prefix server run test:mail"
  }
}
